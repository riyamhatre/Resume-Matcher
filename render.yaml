services:
  - type: web
    name: resume-api
    env: python
    buildCommand: pip install -r requirements.txt && python -m nltk.downloader punkt stopwords && python -m spacy download en_core_web_sm
    startCommand: python app.py
    envVars:
      - key: FLASK_ENV
        value: production
